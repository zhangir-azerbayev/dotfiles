# dotfiles
Configuration files for the following programs
- alacritty
- tmux
- neovim with lua config
- $\LaTeX{}$ preamble and snippets.

To symlink config files to correct locations, run `./link.sh`.
