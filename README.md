# dotfiles
macOS configuration files for the following programs
- alacritty
- zsh
- git
- tmux
- neovim with lua config
- $\LaTeX{}$ preamble and snippets

## Setup
1. Run `./install.sh` to install dependencies via Homebrew.
2. Run `./link.sh` to symlink config files to their expected locations.
